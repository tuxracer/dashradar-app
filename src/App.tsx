const App = () => {
  return (
    <main className="fixed inset-0 flex items-center justify-center bg-surface">
      <h1 className="text-2xl font-semibold tracking-[0.34em] text-white/85">
        DASHRADAR
      </h1>
    </main>
  );
};

export default App;
