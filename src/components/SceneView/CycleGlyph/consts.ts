/**
 * Bicycle and motorcycle frame color: the amber accent, shared with the person
 * glyph because both are a rider in traffic rather than a vehicle around one.
 */
export const CYCLE_COLOR = "#ffb340";
