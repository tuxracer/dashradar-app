/**
 * The uppercase display wordmark shown in the status bar, the error screen,
 * and the intro screen eyebrow. The lowercase share URL and its label live in
 * ShareCard's consts next to the QR code that encodes them.
 */
export const WORDMARK = "DASHRADAR.APP";
