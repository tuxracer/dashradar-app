# Sentry DSN for error + performance monitoring.
# This is a public client key (it ships in the browser bundle), not a secret.
# Leave empty to disable Sentry. Copy this file to .env.local and fill it in for
# local development; set the same variable in the hosting environment (Vercel)
# for production.
VITE_SENTRY_DSN=
